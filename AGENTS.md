# AGENTS.md

<!-- ## Python environment

Use the conda environment named `microstructures` for all Python commands.

Prefer commands like:

```bash
conda run -n microstructures python -m pytest
conda run -n microstructures python scripts/train.py
conda run -n microstructures python -m pip install -e . -->
```